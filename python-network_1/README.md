# python-network_1

This project covers fetching and manipulating HTTP resources in Python
using both the `urllib` and `requests` packages: GET/POST requests,
reading response headers, decoding JSON, and handling HTTP errors.
